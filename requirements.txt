# BlindSpot backend dependencies.
#   pip install -r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
sqlalchemy>=2.0
python-multipart>=0.0.9      # multipart uploads for CSV/Excel/incident files
openpyxl>=3.1                # .xlsx ingestion
numpy>=1.26                  # embeddings and the exact vector store

# Optional. BlindSpot uses FAISS when present and falls back to an exact numpy
# index otherwise — results are identical, only the search path differs.
faiss-cpu>=1.8; platform_machine != "arm64" or sys_platform != "win32"

# Development / tests
pytest>=8.0
httpx>=0.27                  # required by fastapi.testclient
ruff>=0.5
